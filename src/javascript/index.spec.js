describe('index.js', function () {
    // beforeEach(function () {
    //     import 'node_modules/core-js/es/array/from';
    // });

    it('should init to have been called', function () {

        // init();
        // expect(init).toHaveBeenCalled();
    });
});
